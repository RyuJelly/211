$(function(){
	$('.quantity input').change(function(){
		var price = parseFloat($(this).parent().parent().find('.price').text().replace(/^[^\d.]/, ''));
		price = isNaN(price) ? 0 : price;
		
		var quantity = $(this).val();
		quantity = isNan(quantity)? 0 : price;
		alert(quantity);
		
		var cost = 
	});
});



